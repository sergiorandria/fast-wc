# fast-wc

A C++ implementation of GNU Coreutils `wc` (word count).

## About

This is a reimplementation of the `wc` utility in C++, which counts lines, words, characters and bytes in text files.
